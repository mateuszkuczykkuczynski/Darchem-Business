import React from 'react';
import exampleImage from './heroback.jpeg';


const Hero = () => {
    return (

        <div className="bg-auto bg-no-repeat bg-center ..." style={{ backgroundImage: `url(${exampleImage})` }}></div>

);
};

export default Hero;
