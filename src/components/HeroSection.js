import React from 'react';
import {ReactComponent as Logo } from '/logo.svg';

const Hero = () => {
    return (
        <div className="bg-fixed">
            <Logo />
        </div>
    );
};
export default Hero;
